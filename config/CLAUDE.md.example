# CLAUDE.md（团队基线模板）
#
# 这是团队共享的 Claude Code 行为基线。install.sh 不会自动覆盖你的个人
# ~/.claude/CLAUDE.md —— 请把下面需要的段落手动合并进去，再补充你自己的私有项。

## 语言

* 无论使用什么语言提问，都用中文回复。

## 代理访问

命令行通过 127.0.0.1:7890 代理访问互联网；遇到企业内部域名（如 yaduo.com、
at-our.com）时，把它们加入 NO_PROXY。

## 容器镜像

远程私有 Podman 镜像仓库地址：zot.chester.monster

## 数据库

* 查询 PostgreSQL 用 pgcli
* 查询 MySQL 用 mycli

## Java 技术栈（团队基线）

* **JDK**：1.8 / 11 / 17 共存，**手动切换 `JAVA_HOME`**（无 sdkman/jenv）。构建前先切到项目
  所需版本，见 `cli/jdk-multiversion.md`。
* **框架/构建**：Spring Boot；Maven 与 Gradle 并存（Gradle 用 `./gradlew`）。
* **ORM**：MyBatis —— SQL 参数一律 `#{}`，**禁止 `${}` 拼接用户输入**。
* **中间件**：Apollo（配置）/ Eureka（服务发现）/ SkyWalking（APM）/ RocketMQ（消息队列）/
  JetCache（缓存，底层 Redis）。接入时参考 `docs/best-practices.md` 的中间件约定与对应 agent
  实现示例。
* **数据库**：MySQL 用 `mycli`、SQLServer 用 `sqlcmd`/`mssql-cli`，**查库一律 CLI 人工执行**，
  不给 Claude 配数据库直连。
* **可靠性红线**：远程调用必设超时+降级；MQ 消费端必做幂等；注意 `@Transactional` 自调用失效。
* 大型项目可调用 `agents/` 下的 Java agent 团队（tech-lead 统筹，dev/data/test/review/security 分工）。

## 飞书

* 团队主要通过 lark-cli + lark-* skills 操作飞书（文档/表格/日历/IM 等）。
* 用花名在飞书内搜索同事。

## hero 入口

- **hero 入口**：开发类意图（修 bug / 小迭代 / 重构 / 调研 / 性能 / 安全 / PRD）可直接说
  `hero <意图>`，由 hero-dispatch 分诊到对应工作流；也可继续用各 skill 的原触发词直达。

## Agent Teams（tmux 组队）

让多位 Hero（孔明 + 文远 + 希仁等）在 tmux 分屏里并行协作，共用同一仓库上下文。
- 启用条件：在 `~/.claude/settings.json` 配置 `teammateMode: "tmux"` 与
  `env.CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS: "1"`，详见 `config/settings.json.example`
- 分诊命令：`hero 组队 [孔明+文远+希仁]` 或 `hero team`
- 启动步骤：先 `tmux new -s work` 建会话，再在目标 pane 执行 `claude --agent <agent 名>`
- 推荐组合：孔明（tech-lead, 规划）+ 文远（backend-dev, 实现）+ 希仁（test-engineer, 测试）

## 个人段落（按需自行补充）

<!-- 例如：你的花名、真名、邮箱、个人偏好等。不要把私密信息提交回本仓库。 -->
