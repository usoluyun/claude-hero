# 本地实际配置（含密钥），不入库；仓库只放 *.example
.mcp.json
config/CLAUDE.md
config/settings.json

# Worktree 隔离工作区（git worktree，每个 PRD 独占）
.worktrees/
worktrees/

# 临时验证分支（跨需求合并时创建）
validate/

# 备份与临时
*.bak.*
.DS_Store

# 演练用的临时 HOME
/tmp-home/
